<pre class="fxguide">
    Logical Formula:
    An expression resulting a boolean value.

    Example:
    1) To formulate Elibility based on Employment Type and Gender:
    	{stakeholder.employmenttype} == 0 && {stakeholder.gender} == 1

    =========================================
    @Html.Partial("_FxOperators")
    =========================================
    @Html.Partial("_FxVariables")
</pre>